//file
import sys.FileSystem;
import sys.io.File;
import sys.FileStat;
//path
import haxe.io.Path;
using StringTools;
//html
import haxe.xml.Fast;
import haxe.xml.Parser;
class Main {
	static function main() 
	{
		/**
		 * Notes
		 * FileSystem.stat(path);
		 * FileSystem.rename(path,newpath);
		 * File.copy(path,newpath);
		 */
		//simple scripting environment in order to maniputlate data if need be.
		trace("start script");
		//script
	}
}
